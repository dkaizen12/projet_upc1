import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF67A896);       // card, thème, back si image n'apparait pas 
  static const Color secondary = Color(0xFF90EE90);     // barre de progression 
  static const Color accent = Color(0xFFD32F2F);        // Rouge (likes, deconnexion)

  static const Color backgroundLight = Color(0xFFF9F9F9); //arrière plan 
  static const Color backgroundDark = Color(0xFF121212); //fond sombre 

  static const Color textPrimary = Color(0xFF000000); //text principal
  static const Color textSecondary = Color(0xFF555555);// text secondaire 

  static const Color divider = Color(0xFF67A896); // barre de separation 
  static const Color fav = Color(0xFF1E90FF); //bouton favoris

  static const Color error = Colors.redAccent; //erreur, alert, snack
}